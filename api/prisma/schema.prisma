generator client {
  provider = "prisma-client-js"
}

datasource default {
  provider = "postgresql"
}

model Message {
  id        String   @id @default(uuid())
  content   String
  username  String
  room      String   @default("Каб 104")
  createdAt DateTime @default(now())
}
